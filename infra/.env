# Picked up automatically by `docker compose` when run from this directory.
# Matches the host UID so the Airflow containers (bind-mounting the repo at
# /opt/airflow/project) can write validation/output, models/output, and
# reports/ without a permissions mismatch.
AIRFLOW_UID=1000
