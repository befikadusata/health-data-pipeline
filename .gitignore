__pycache__/
*.pyc
.venv/
.env
# infra/.env has no secrets (just AIRFLOW_UID) and is needed for
# `docker compose up` to work out of the box - tracked deliberately.
!infra/.env

# local run artifacts - regenerated by data_gen/generate.py, warehouse/ingest.py, etc.
data_gen/output/
validation/output/
models/output/
reports/
mlflow.db
mlruns/

# uv
.python-version.local
